<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened runtime, no App Sandbox.

    com.apple.security.app-sandbox is deliberately absent: the sandbox blocks
    CGEvent posting to other processes and reading other apps' window geometry,
    which is everything this app does. A sandboxed build launches cleanly and
    then silently fails at every action.

    Electron needs the JIT and unsigned-memory entitlements for V8.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Bringing other applications to the front. -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
</dict>
</plist>
